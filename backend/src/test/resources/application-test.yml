spring:
  datasource:
    url: jdbc:sqlite::memory:?cache=shared
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
